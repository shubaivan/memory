{% extends "::base.html.twig" %}

{% block title %} Альбом {{ album.name }} {% endblock %}

{% block content %}
<div class="col-lg-12" xmlns="http://www.w3.org/1999/html">
    {% if is_granted('ROLE_USER') %}


        <div class="col-sm-12" id="featured">
            <div class="page-header text-muted">
                <a href="">Редагувати інформацію про альбом</a>
            </div>
        </div>


    {% endif %}

    <a href="{{ path('app_add_song', {'slug' : album.slug}) }}" class="btn btn-success btn-large"><span class="glyphicon glyphicon-plus"></span>  Додати новий трек</a></br>

<!--/top story-->
<div class="row">
    <div class="col-sm-10">
        <h3>{{ album.name }}</h3>
        <h4><span class="label label-default">Рік випуску: {{ album.year }}</span></h4>
        <small class="text-muted">Короткий опис:{{ album.text }}</small>
            <h4><span class="label label-default">List song</span></h4>


        {% for song in album.song %}

            <li><small class="text-muted"><a href="{{ path('app_get_all_videos_in_song', {'slugAlbum' : album.slug, 'slug' : song.slug}) }}">{{ song.name }}</a></small></li>

        {% endfor %}

        <a href="{{ path('app_get_all_albums')}}" class="btn btn-success btn-large"><i class="glyphicon glyphicon-backward"></i> Back to tha all albums</a></br>
    </div>
    <div class="col-sm-2">
        <a href="{{ path('app_get_single_album', {"slug" : album.slug}) }}" class="pull-right"><img src="http://amuzic.ru/common/images/artist/7572688069150/album_60672545641341.jpg"></a>
    </div>
</div>



    {#<div class="col-lg-12">{{ album.name }}</div>#}

    {#<div class="col-lg-12">#}
        {#<div class="col-lg-4">#}
            {#<img src="#">#}
        {#</div>#}

        {#<div class="col-lg-8">#}
            {#<p>#}
                {#Рік випуску: {{ album.year }}#}
            {#</p>#}

            {#<p>#}
                {#Короткий опис: {{ album.text }}#}
            {#</p>#}
        {#</div>#}

        {#<div class="col-lg-12">#}
            {#<div>#}
                {#<a href="{{ path('app_add_song', {'slug' : album.slug}) }}">Додати новий трек</a>#}
            {#</div>#}

            {#<h3>Songs</h3>#}

            {#{% for song in album.song %}#}
                {#<a href="{{ path('app_get_all_videos_in_song', {'slugAlbum' : album.slug, 'slug' : song.slug}) }}">#}
                    {#<div>#}
                        {#{{ song.name }}#}
                    {#</div>#}
                {#</a>#}
            {#{% endfor %}#}
        {#</div>#}
    {#</div>#}
</div>
{% endblock %}
