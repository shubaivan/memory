{% extends "::base.html.twig" %}

{% block title %} Альбоми группи Скрябін {% endblock %}

{% block content %}
    {% if is_granted('ROLE_USER') %}

    <div class="col-lg-12">
        <a href="{{ path('app_add_album') }}">Додати альбом</a>
    </div>

    {% endif %}

    <div class="col-sm-12" id="featured">
    <div class="page-header text-muted">
    Album
    </div>
    </div>


    {% for album in albums %}
    <div class="row">
        <div class="col-sm-10">
            <h3>{{ album.name }}</h3>
            <h4><span class="label label-default"><a href="{{ path('app_get_single_album', {'slug': album.slug }) }}">{{ album.name }}</a></span></h4><h4>
            <small class="text-muted">Короткий опис: {{ album.text|slice(0, 255) }} <a href="{{ path('app_get_single_album', {'slug': album.slug }) }}">Read More</a></small>
            </h4>
            </div>
            <div class="col-sm-2">
                <a href="{{ path('app_get_single_album', {"slug" : album.slug}) }}" class="pull-right"><img src="{{ asset('bundles/app/images/11.jpg') }}" class="img-circle"></a>
        </div>
    </div>


        {#{% for album in albums %}#}
        {#<div>#}
            {#<div class="col-lg-12">#}
                {#<a href="{{ path('app_get_single_album', {'slug': album.slug }) }}">{{ album.name }}</a>#}
            {#</div>#}

            {#<div class="col-lg-4">#}
                {#<a href="{{ path('app_get_single_album', {"slug" : album.slug}) }}" class="pull-right"><img src="{{ asset('bundles/app/images/ui-sam.jpg') }}" class="img-circle"></a>#}
            {#</div>#}

            {#<div class="col-lg-8">#}
                {#<p>#}
                    {#Рік випуску: {{ album.year }}#}
                {#</p>#}

                {#<p>#}
                    {#Короткий опис: {{ album.text|slice(0, 255) }}#}
                {#</p>#}
            {#</div>#}
        {#</div>#}

    {% endfor %}
{% endblock %}
