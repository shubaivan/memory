{% extends "AppBundle::layout.html.twig" %}

    {% block content %}

        {{ form_start(form) }}
        {{ form_errors(form) }}
        <div>
            {{ form_label(form.link) }} <br>
            {{ form_widget(form.link) }}
        </div>

        <div style="padding-bottom: 20px;">
            {{ form_label(form.album) }} <br>
            {{ form_widget(form.album) }}
        </div>

        {{ form_end(form) }}

    {% endblock %}
