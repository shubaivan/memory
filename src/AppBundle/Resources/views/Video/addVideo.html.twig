{% extends "::base.html.twig" %}

{% block title %} addVideo {% endblock %}

{% block content %}
    <div class="col-lg-12">
        {{ form_start(form) }}
            <div class="col-lg-12">
                {{ form_row(form.name) }}
            </div>

            <div class="col-lg-12">
                {{ form_row(form.link) }}
            </div>

            <div class="col-lg-12">
                <button type="submit">Додати</button>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
