{% extends "AppBundle::layout.html.twig" %}

    {% block title %} {{ parent() }} Blog {% endblock %}
    {% block content %}
        {% for video in videos %}
            {{ video.Title }} </br>
            {{ video.Link | raw}} </br>
            {{ video.album.name }} </br>
            {{ video.chord.description }} </br>
            </br>
        {% endfor %}
    {% endblock %}
