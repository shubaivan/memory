<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <parameters>
        <parameter key="app.example.class">AppBundle\Example</parameter>

        <parameter key="app.song.repository.class">AppBundle\Repository\SongRepository</parameter>
        <parameter key="app.search.song.class">AppBundle\Service\Search</parameter>
    </parameters>

    <services>
        <service id="doctrine_mongo_db_param_converter" class="Sensio\Bundle\FrameworkExtraBundle\Request\ParamConverter\DoctrineParamConverter">
            <argument type="service" id="doctrine_mongodb" />
            <tag name="request.param_converter" converter="doctrine.odm" />
        </service>

        <service id="app.song.repository"
                 class="%app.song.repository.class%"
                 factory-service="doctrine.odm.mongodb.document_manager"
                 factory-method="getRepository">
            <argument>AppBundle:Song</argument>
        </service>

        <service id="searcher"
                 class="%app.search.song.class%">
            <argument type="service" id="app.song.repository" />
        </service>

    </services>

</container>
